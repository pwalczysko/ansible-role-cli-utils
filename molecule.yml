---
dependency:
  name: galaxy
  requirements_file: tests/requirements.yml
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-cli-utils
      image: centos
      image_version: 7
      ansible_groups:
        - group1
verifier:
  name: testinfra
